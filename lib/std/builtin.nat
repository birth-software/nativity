const Os = enum{
    linux,
    macos,
    windows,
};

const Cpu = enum{
    aarch64,
    x86_64,
};

const Abi = enum{
    none,
    gnu,
    msvc,
};

const CallingConvention = enum{
    system_v,
    naked,
};
