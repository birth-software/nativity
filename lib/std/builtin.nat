const Os = enum{
    linux,
    macos,
    windows,
    freestanding,
};

const Cpu = enum{
    x86_64,
    aarch64,
};

const Abi = enum{
    none,
    gnu,
    msvc,
};

const CallingConvention = enum{
    system_v,
};
